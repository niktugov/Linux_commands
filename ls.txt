# ls -ltr - список по дате (новые снизу)
# ls -lh - human 
# ls -l ?abc* - вывод с одним любым символом до abc и любыми после
# ls -l | more - выполнит постранично ls
# ls -l | tail -2  -выведет последние 2 строки ls 
# lscpu
# lsmem
# lsof -i -P -n | grep 22 - инф о портах
# ls -l .. 
# ll /proc | wc -l - сколько процессов в данный момент в системе

# yum install lshw
# lshw — инфо о железе

# ls -ld — инфо о папке

# lsattr — расширенные атрибуты файла 
# chattr 

